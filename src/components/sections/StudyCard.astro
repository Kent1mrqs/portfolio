
---
import { getCollection } from "astro:content";
import Card from '../Card.astro'
import { Button } from '../ui/button'
import { formateLocalDate } from "../../lib/utils";

let studies = await getCollection('studies');

// sort projects by dateStart DESC
studies.sort((a: any, b: any) => {
  return new Date(b.data.startDate) - new Date(a.data.startDate);
});

studies = studies.slice(0, 3);
---


<div class="mt-2 space-y-4">
  {
    studies.slice(0, 3).map((exp) => (
      <div>
        <h4 class="text-md font-medium">{exp.data.title}</h4>
        <a class="text-sm text-primary hover:underline" href={ '/studies/' + exp?.slug ?? ''}>
          {exp.data.company}
        </a>
        <p class="text-sm text-gray-600">
         {formateLocalDate(exp.data.startDate)} {exp.data?.endDate ? '- ' + formateLocalDate(exp.data.endDate) : ''}

        </p>
      </div>
    ))
  }
  <a href="/studies">
    <Button variant="link" className="pl-0"> View More</Button>
  </a>
</div>

